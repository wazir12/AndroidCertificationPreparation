<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/color_orange</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
<style name="courseTitle" parent="@android:style/TextAppearance.Material.Large" tools:targetApi="lollipop">
    <item name="android:textStyle">bold</item>
    <item name="android:textColor">@color/color_orange</item>
</style>
<style name="addFab" parent="@style/Widget.Design.FloatingActionButton">
    <item name="android:drawable">@drawable/ic_baseline_add_circle_24</item>
    <item name="backgroundTint">@color/color_transparent</item>
    <item name="tint">@color/color_orange</item>
</style>
</resources>